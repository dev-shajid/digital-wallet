namespace WalletSystem.IntegrationTests;

/// <summary>
/// One real test to prove the test project is wired up correctly (references the API
/// project, runs under xUnit). Real integration tests (spinning up the app + hitting
/// endpoints) are added in a later phase once business endpoints exist.
/// </summary>
public class PlaceholderTests
{
    [Fact]
    public void Placeholder_AlwaysPasses()
    {
        Assert.True(true);
    }
}
